import os

CONFIG_FILE_PATH = os.path.join("config", "config.yaml")
PARAMS_FILE_PATH = os.path.join("params.yaml")  # Optional YAML for hyperparams
SCHEMA_FILE_PATH = os.path.join("schema.yaml")  # Optional schema file if needed


